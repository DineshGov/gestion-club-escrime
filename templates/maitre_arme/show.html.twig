{% extends 'base.html.twig' %}

{% block title %}MaitreArme{% endblock %}

{% block body %}
    <h1>MaitreArme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ maitre_arme.id }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('maitre_arme_index') }}">back to list</a>

    <a class="btn btn-info" href="{{ path('maitre_arme_edit', {'id': maitre_arme.id}) }}">edit</a>

    {{ include('maitre_arme/_delete_form.html.twig') }}
{% endblock %}
