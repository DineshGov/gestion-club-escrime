{% extends 'base.html.twig' %}

{% block title %}Edit Presence{% endblock %}

{% block body %}
    <h1>Edit Presence</h1>

    {{ include('presence/_form.html.twig', {'button_label': 'Update'}) }}<br>

    <a class="btn btn-info" href="{{ path('presence_index') }}">back to list</a>

    {{ include('presence/_delete_form.html.twig') }}
{% endblock %}
