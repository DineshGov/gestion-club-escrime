{%  block body %}
    <div class="container" style="margin-top: 2%">

            {{ form_start(form) }}
            <div class="offset-3 col-sm-6">
                {{ form_label(form.categorie) }}
                {{ form_widget(form.categorie) }}
                {{ form_errors(form.categorie) }}
            </div>
        <br>
        <div class="row">

            <div class="offset-3 col-sm-3">

                <a class="btn btn-info btn-lg" href="{{ path('niveau_index') }}">Retour</a>
            </div>
            <div class="col-sm-3">

                <button class="btn btn-lg btn-success " style="margin-left: 20%">{{ button_label|default('Ajouter un niveau') }}</button>
            </div>
            {{ form_end(form) }}

        </div>
    </div>

{%  endblock %}