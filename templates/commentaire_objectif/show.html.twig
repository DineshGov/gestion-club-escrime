{% extends 'base.html.twig' %}

{% block title %}CommentaireObjectif{% endblock %}

{% block body %}
    <h1>CommentaireObjectif</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire_objectif.id }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ commentaire_objectif.commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commentaire_objectif_index') }}">back to list</a>

    <a href="{{ path('commentaire_objectif_edit', {'id': commentaire_objectif.id}) }}">edit</a>

    {{ include('commentaire_objectif/_delete_form.html.twig') }}
{% endblock %}
