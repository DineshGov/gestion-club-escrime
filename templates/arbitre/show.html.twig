{% extends 'base.html.twig' %}

{% block title %}Arbitre{% endblock %}

{% block body %}
    <h1>Arbitre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ arbitre.id }}</td>
            </tr>
        </tbody>
    </table>

    <a <a class="btn btn-lg btn-primary" href="{{ path('arbitre_index') }}">back to list</a>

    <a <a class="btn btn-lg btn-info" href="{{ path('arbitre_edit', {'id': arbitre.id}) }}">edit</a>

    {{ include('arbitre/_delete_form.html.twig') }}
{% endblock %}
