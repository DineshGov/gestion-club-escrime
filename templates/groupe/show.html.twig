{% extends 'base.html.twig' %}

{% block title %}Groupe{% endblock %}

{% block body %}
    <h1>Groupe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ groupe.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ groupe.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('groupe_index') }}">back to list</a>

    <a class="btn btn-info" href="{{ path('groupe_edit', {'id': groupe.id}) }}">edit</a>
    <br>

    {{ include('groupe/_delete_form.html.twig') }}
{% endblock %}
