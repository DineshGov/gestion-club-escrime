{% extends 'base.html.twig' %}

{% block title %}Tireur{% endblock %}

{% block body %}
    <h1>Tireur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tireur.id }}</td>
            </tr>
            <tr>
                <th>Blason</th>
                <td>{{ tireur.blason }}</td>
            </tr>
            <tr>
                <th>NiveauSurclassement</th>
                <td>{{ tireur.niveauSurclassement }}</td>
            </tr>
            <tr>
                <th>Arme</th>
                <td>{{ tireur.armes }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tireur_index') }}">back to list</a>

    <a href="{{ path('tireur_edit', {'id': tireur.id}) }}">edit</a>

    {{ include('tireur/_delete_form.html.twig') }}
{% endblock %}
